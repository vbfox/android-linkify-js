pre-commit:
  parallel: true
  jobs:
    - name: oxfmt
      glob: "*.{ts,js,mjs,cjs,json,jsonc,yml,yaml,md}"
      run: yarn oxfmt --no-error-on-unmatched-pattern {staged_files}
      stage_fixed: true
    - name: oxlint
      glob: "*.{ts,js,mjs,cjs}"
      run: yarn oxlint --no-error-on-unmatched-pattern {staged_files}
