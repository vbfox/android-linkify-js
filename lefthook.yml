pre-commit:
  parallel: true
  jobs:
    - name: oxfmt
      glob: "*.{ts,js,mjs,cjs,json,jsonc,yml,yaml,md}"
      run: yarn oxfmt {staged_files}
      stage_fixed: true
    - name: oxlint
      glob: "*.{ts,js,mjs,cjs}"
      run: yarn oxlint {staged_files}
